# 任务清单：双层知识库 —— claude-howto

> 创建于：2026-04-01

---

## 里程碑

| # | Milestone | Target | Description |
|---|-----------|--------|-------------|
| M1 | Infrastructure Live | 2026 年 4 月底 | quickstart.sh、CLAUDE.md、staleness action、agent index generator 上线；升级 2 个最薄弱模块 |
| M2 | 6/10 Modules Complete | 2026 年 6 月底 | Slash commands、memory、skills、CLI 模块全部升级完成；生成器输出合法 manifest |
| M3 | All 10 Modules Complete | 2026 年 8 月底 | 所有模块都具备场景化引导、Try It Now 区块、决策树和命名模式 |
| M4 | Agent Layer Live | 2026 年 9 月底 | 生成 agent-manifest.json + AGENT-INDEX.md；仓库内有 lookup skill；RECIPES.md + COMMUNITY-PROJECTS.md 上线 |
| M5 | Version Audit Complete | 2026 年 11 月底 | 所有 10 个模块按当前 CC 版本验证；RECIPES.md 达到 8+ 条 recipes |
| M6 | Self-Sustaining System | 2027 年 3 月底 | Agent manifest 覆盖 100% 模块（CI 校验）；RECIPES.md 达到 10+；社区页自然增长 |

---

## Phase 1 — 基础设施与薄弱模块（2026 年 4 月）

**目标里程碑：M1**

### Pillar 4：新手引导

- [ ] **T-001** —— 编写 `scripts/quickstart.sh`
  - 检测 `~/.claude/` 目录（若缺失，在获得用户确认后创建）
  - 将第一个 slash command + CLAUDE.md + skill 复制到用户环境
  - 向 CLAUDE.md 追加 agent-discovery 行，链接到 AGENT-INDEX.md
  - 必须幂等：已有文件只警告跳过，绝不覆盖
  - 完成后打印下一步操作

- [ ] **T-002** —— 创建 `QUICKSTART.md`（“前 15 分钟上手”可视化指南）
  - 使用带注释的终端步骤代码块（尽量避免 PNG 截图；优先 ASCII 或 Mermaid）
  - 记录每张图对应由哪个命令生成，方便后续重录

- [ ] **T-003** —— 给全部 10 个模块 README 增加难度徽章
  - 格式：`> 🟢 **Beginner** | ⏱ 30 minutes`
  - 在徽章下方增加 “What you'll build” 预览要点

### Pillar 7：持续演进课程

- [ ] **T-004** —— 创建 `WHATS-NEW.md`
  - 格式：`## CC vX.X — DATE`，每个功能变化用项目符号列出
  - 至少补上一条当前 CC 版本记录

- [ ] **T-005** —— 给全部 10 个模块 README 增加版本徽章
  - 格式：`> ✅ Verified against Claude Code **vX.X.X** · Last verified: YYYY-MM-DD`
  - 给每个模块的 frontmatter 添加 `cc_version_verified`

- [ ] **T-006** —— 创建 `.github/workflows/staleness-check.yml`
  - 调度：每周一 09:00 UTC
  - 读取每个模块 frontmatter 中的 `last_verified` 日期（用 `yq` 或一个 10 行左右的 Python 脚本）
  - 若某模块 30+ 天未验证，则自动创建 GitHub Issue
  - 如果已有同标题未关闭 issue，则跳过

### Pillar 6：内容质量（CLAUDE.md）

- [ ] **T-007** —— 在仓库根目录创建 `CLAUDE.md`
  - 作为项目自身配置，示范最佳实践
  - 记录贡献规范、模块结构和维护流程

### Pillar 2：AI Agent Index（生成器）

- [ ] **T-008** —— 编写 `scripts/build-agent-index.py`
  - 读取全部 10 个模块 README
  - 提取：标题（→ topics）、代码块（→ 带语言标签的示例）、表格（→ 参考数据）
  - 输出 schema 与 CEO 计划一致的 `agent-manifest.json`
  - 输出 `AGENT-INDEX.md`（面向人的摘要）
  - 校验引用是否都指向真实文件

### 模块 08-checkpoints：完整深度升级（311 → 800+ 行）

- [ ] **T-009** —— 用真实场景重写模块引言
  - 例子："Your refactor just broke 3 things. Here's how checkpoints save you..."

- [ ] **T-010** —— 添加 checkpoint 策略决策树（Mermaid flowchart）
  - “I want to do X” → 按箭头走 → 落到对应 checkpoint 模式

- [ ] **T-011** —— 添加 3 个工作流模板
  - 安全重构工作流
  - 用 checkpoints 做 A/B 测试
  - 灾难恢复工作流

- [ ] **T-012** —— 增加与 planning mode 集成的章节

- [ ] **T-013** —— 增加 checkpoint 生命周期 Mermaid 图

- [ ] **T-014** —— 增加 2+ 个 “Try It Now” 区块，包含可验证命令
  - 每个区块都要有：上下文说明、可执行命令、预期输出描述

- [ ] **T-015** —— 增加 “Patterns & Recipes” 章节，包含 2+ 个命名模式
  - 每个模式包含：problem、solution code、when to use、when NOT to use

### 模块 06-hooks：完整深度升级

- [ ] **T-016** —— 用真实场景重写模块引言
  - 例子："Your CI caught a lint error after 20 minutes. Hooks catch it in 2 seconds..."

- [ ] **T-017** —— 增加 hooks 决策树（Mermaid flowchart）
  - “I want to trigger X when Y happens” → 按箭头走 → 落到对应 hook 类型

- [ ] **T-018** —— 增加 5 个完整 hook 示例，带完整脚本（每个约 80 行）
  1. 写入时自动格式化（prettier/black）
  2. 提交时安全扫描（trufflehog/bandit）
  3. 文件变化时运行测试
  4. 会话结束时发送通知
  5. Prompt 校验（拦截危险模式）

- [ ] **T-019** —— 增加 2+ 个 “Try It Now” 区块，包含可验证命令

- [ ] **T-020** —— 增加 “Patterns & Recipes” 章节，包含 2+ 个命名模式

**M1 Checkpoint：基础设施上线，两个最薄弱模块完成深度升级**

---

## Phase 2 — 深度升级：强势模块第一批（2026 年 5–6 月）

**目标里程碑：M2**

### 模块 01-slash-commands：完整深度升级

- [ ] **T-021** —— 用场景重写引言（"Your teammate just pushed 47 files..."）
- [ ] **T-022** —— 增加决策树（Mermaid）
- [ ] **T-023** —— 增加 2+ 个 “Try It Now” 区块
- [ ] **T-024** —— 增加 “Patterns & Recipes” 章节，包含 2+ 个命名模式

### 模块 02-memory：完整深度升级

- [ ] **T-025** —— 用真实场景重写引言
- [ ] **T-026** —— 增加决策树（Mermaid）
- [ ] **T-027** —— 增加 2+ 个 “Try It Now” 区块
- [ ] **T-028** —— 增加 “Patterns & Recipes” 章节，包含 2+ 个命名模式

### 模块 03-skills：完整深度升级

- [ ] **T-029** —— 用真实场景重写引言
- [ ] **T-030** —— 增加决策树（Mermaid）
- [ ] **T-031** —— 增加 2+ 个 “Try It Now” 区块
- [ ] **T-032** —— 增加 “Patterns & Recipes” 章节，包含 2+ 个命名模式

### 模块 10-cli：完整深度升级

- [ ] **T-033** —— 用真实场景重写引言
- [ ] **T-034** —— 增加决策树（Mermaid）
- [ ] **T-035** —— 增加 2+ 个 “Try It Now” 区块
- [ ] **T-036** —— 增加 “Patterns & Recipes” 章节，包含 2+ 个命名模式

### 生成器校验

- [ ] **T-037** —— 每次模块升级后运行 `scripts/build-agent-index.py`；确认输出的 manifest 合法
- [ ] **T-038** —— 增加 CI 步骤：每次 push 校验 agent-manifest.json schema

**M2 Checkpoint：完成 6/10 模块，生成器可输出合法 manifest**

---

## Phase 3 — 深度升级：强势模块第二批（2026 年 7–8 月）

**目标里程碑：M3**

### 模块 04-subagents：完整深度升级

- [ ] **T-039** —— 用真实场景重写引言
- [ ] **T-040** —— 增加决策树（Mermaid）
- [ ] **T-041** —— 增加 2+ 个 “Try It Now” 区块
- [ ] **T-042** —— 增加 “Patterns & Recipes” 章节（例如 "The Multi-Agent Review Pattern"）

### 模块 05-mcp：完整深度升级

- [ ] **T-043** —— 用真实场景重写引言
- [ ] **T-044** —— 增加决策树（Mermaid）
- [ ] **T-045** —— 增加 2+ 个 “Try It Now” 区块
- [ ] **T-046** —— 增加 “Patterns & Recipes” 章节，包含 2+ 个命名模式

### 模块 07-plugins：完整深度升级

- [ ] **T-047** —— 用真实场景重写引言
- [ ] **T-048** —— 增加决策树（Mermaid）
- [ ] **T-049** —— 增加 2+ 个 “Try It Now” 区块
- [ ] **T-050** —— 增加 “Patterns & Recipes” 章节，包含 2+ 个命名模式

### 模块 09-advanced-features：完整深度升级

- [ ] **T-051** —— 用真实场景重写引言
- [ ] **T-052** —— 增加决策树（Mermaid）
- [ ] **T-053** —— 增加 2+ 个 “Try It Now” 区块
- [ ] **T-054** —— 增加 “Patterns & Recipes” 章节，包含 2+ 个命名模式

**M3 Checkpoint：10 个模块全部完成且风格一致**

---

## Phase 4 — Agent 层与跨模块内容（2026 年 9 月）

**目标里程碑：M4**

### Pillar 2：完成 AI Agent Index

- [ ] **T-055** —— 针对 10 个完整模块运行 `scripts/build-agent-index.py`
  - 生成覆盖 100% 模块的最终版 `agent-manifest.json`
  - 生成 `AGENT-INDEX.md`，并从 README.md 链接过去

- [ ] **T-056** —— 创建 `skills/claude-code-guide-lookup/SKILL.md`
  - 读取 `agent-manifest.json` 查找相关模块
  - 使用 `Read` 加载对应章节
  - 安装方式：`cp -r claude-code-guide/skills/claude-code-guide-lookup ~/.claude/skills/`

- [ ] **T-057** —— 在 quickstart.sh 和 QUICKSTART.md 中把 lookup skill 作为可选能力提及

### Pillar 3b：跨模块 RECIPES.md

- [ ] **T-058** —— 创建 `RECIPES.md`，包含 5+ 个复合工作流
  - 每条 recipe 的 schema：name、modules-used、problem、solution code block、expected outcome
  - 示例：skill + hook + subagent = 自动化审查流水线
  - 所有主要标题都要有 HTML anchors，方便 deep-linking

### Pillar 5：Community Showcase
